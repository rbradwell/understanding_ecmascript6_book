/** 
 *
 * ES6 defines the syntax for mudules but leaves loading to environment.
 * Obviously different for all Javascript environments.
 * the loading mechanism is abstracted to an undefined internal operation called HostResolveImportModule. 
 * 
 * p293 outlines how, in the browser, the script tag has been update to use modules
 * p296 outlines how to load modules as web workers
 * p297 outlines how to specify path in browser for modules.
 **/